pipeline {
  agent any
  stages {

    stage ('Initialize') {
      steps {
        sh '''
                    echo "${python3 --version}"
                    echo "Hello world"
            '''
      }
    }
  }
}
