pipeline {
  agent any
  stages {

    stage ('Initialize') {
      steps {
        sh '''
                    python3 --version
                    echo "Hello world"
            '''
      }
    }
  }
}
